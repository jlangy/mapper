<%- include partials/_head.ejs%>

<link rel="stylesheet" href="/styles/queries.css" type="text/css">
<link rel="stylesheet" href="/styles/browse_maps.css" type="text/css">



<%- include partials/_nav.ejs %>

    <main>
      <br>

      <% if (mapType === 'myMaps') { %>
        <h2>Your Maps:</h2>
      <% } else if (mapType === 'favourites') { %>
        <h2>Your Favourite Maps:</h2>
      <% } else { %>
        <h2>Public Maps:</h2>
      <% } %>

      <div class="browse-maps">
        <% for (const map of JSON.parse(dbData)) {%>
          <div class="map-article">
            <div>
          <h3><%= map.title %></h3>
          <p><%= map.description %></p>
        </div>
          <div class='map-buttons'>
            <a class="view btn btn-outline-dark my-2 my-sm-0" type="submit" href="/maps/<%=map.id%>"><button>View</button></a>
            <% if (mapType === 'myMaps') { %>
              <a class="view btn btn-outline-dark my-2 my-sm-0" type="submit" href="/maps/<%=map.id%>/edit"><button>Edit</button></a></div>
            <% } %>
          </div>
        </div>
        <% }%>

      </div>
    </main>
  </body>
</html>
